name: Deploy Frontend

on:
  push:
    branches: [ "main" ]

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 🔐 Login to GHCR
      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      # 🐳 Build & Push Frontend Image
      - name: Build & Push Frontend Image
        run: |
          docker build -t ghcr.io/${{ github.repository_owner }}/ship_frontend:latest .
          docker push ghcr.io/${{ github.repository_owner }}/ship_frontend:latest

      # 🚀 Deploy to Droplet
      - name: Deploy to Droplet
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.DROPLET_IP }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            docker pull ghcr.io/${{ github.repository_owner }}/ship_frontend:latest

            docker stop frontend || true
            docker rm frontend || true

            docker run -d \
              -p 3000:3000 \
              --name frontend \
              -e NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }} \
              ghcr.io/${{ github.repository_owner }}/ship_frontend:latest